import * as React from "react";


export interface ISideNav {
    ID: number;
    img: string | any;
    Name: string;
    Tab: string[];
}

const SideNavComponent = (props: any) => {
    //   const sideNavArr: ISideNav[] = menuData;

    return (
        <div>
            <p>Karthikeyan</p>
        </div>
    );
};

export default SideNavComponent;
